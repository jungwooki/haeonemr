# 문진 결과별 이미지 첨부 · OCR 연결

사용 순서: **HAEON EMR → 1824 → 환자 선택 → 문진 기록 선택 → 결과 화면에서 이미지 첨부 → 검사지 읽기 → 원본 확인·수정 → 확인 후 이 문진 기록에 반영**.

환자 이름을 클릭한 기본 화면에는 업로드 영역이 없습니다. 7종 문진 결과 화면에서 해당 기록별로 최대 3개 이미지를 첨부합니다. 같은 환자라도 다른 문진의 첨부와 검사 수치는 분리됩니다.

## Google에서 한 번만 설정하세요

1. 문진 기록이 저장되는 구글시트에서 **확장 프로그램 → Apps Script**를 엽니다.
2. 기존 코드를 백업한 뒤 이 폴더의 **Code.gs 전체 내용**을 복사하여 Google의 Code.gs 전체와 교체하고 저장합니다.
3. Apps Script 왼쪽 **서비스** 옆의 **+** 버튼을 누릅니다. **Drive API**를 찾아 **버전 v3**, 식별자 **Drive**를 선택하고 **추가**합니다. 일반 기본 Google Cloud 프로젝트에서는 이 작업으로 연결됩니다. 별도의 표준 Cloud 프로젝트를 사용하는 경우 해당 프로젝트에서 Google Drive API도 활성화해야 합니다.
4. 위쪽 함수 선택 목록에서 **setupEmrOcr**를 선택하여 **실행**합니다. 본인의 Google 계정을 선택하고 Drive·스프레드시트·문서 접근 권한을 허용합니다. 실행 완료가 표시되어야 합니다.
5. **배포 → 배포 관리 → 기존 웹 앱 선택 → 연필 아이콘 → 버전: 새 버전 → 배포**를 누릅니다. 현재 웹 앱 URL은 그대로 유지합니다.
6. 수정된 HTML을 브라우저에서 새로고침한 뒤 위 사용 순서로 시험합니다.

별도의 AI API 키를 입력할 필요는 없습니다. Drive의 이미지→Google 문서 변환 OCR을 사용합니다. OCR용 임시 문서는 작업 후 Drive 휴지통으로 이동합니다. 원본은 비공개 Drive 폴더에 보관합니다.

## 자동 읽기와 반영 범위

- 이미지 첨부: JPG, PNG, GIF, WEBP / 파일당 10MB / 문진 기록별 최대 3개.
- 자동 읽기: JPG, PNG, GIF. WEBP는 미리보기만 지원하며 JPG/PNG로 변환해 첨부하면 읽을 수 있습니다.
- 현재 수치 추출은 제시한 체성분 검사지(BWA 등)의 명시된 항목 이름을 기준으로 합니다. 키, 체중, 골격근량, 체지방량·률, BMI, 체수분, 단백질, 무기질, 성장점수, 전신 위상각, 검사일시, 생년월일, 성별, 회원번호를 추출 대상으로 합니다.
- 원문에서 항목과 값을 명확히 연결하지 못하거나 서로 다른 값이 발견되면 빈칸으로 표시합니다. 그래프나 기준 범위에서 값을 추측하지 않습니다. 다른 검사 종류의 숫자는 원문에 표시되지만 아직 전용 항목으로 자동 분류하지 않습니다.
- 추출된 숫자는 초안입니다. 원본 사진과 대조하여 수정하고 확인 체크박스를 선택한 뒤 반영합니다.
- 반영한 수치는 **해당 문진 결과 화면의 ‘확인된 검사 수치’**로 저장·재조회됩니다. 기존 설문 응답이나 환자 기본정보를 덮어쓰거나 원래 설문 점수를 재계산하지 않습니다. 검사지 생년월일과 기록의 생년월일이 다르면 확인 안내가 표시됩니다.
- 원본 검사지는 배포 후 실제 자료로 확인해야 합니다. OCR 출력의 표 배치가 달라질 수 있으므로 샘플 이미지의 모든 항목이 반드시 자동 추출되는 것은 아닙니다.

## 기존 자료와 호환성

이번 버전은 `haeon-images-v2`입니다. 서버가 이전 버전이면 업로드를 차단하고 업데이트 안내를 표시합니다. 구버전 서버로 잘못된 저장 요청을 보내지 않습니다.

새 자료는 `record_images` 탭에 문진 식별자(이름·기록 시각·종류)로 저장됩니다. 기존 `patient_images` 탭과 Drive 원본은 보존하며, 어떤 문진에 속하는지 확인 없이 자동 배정하지 않습니다. 예전에 환자 전체에 첨부한 자료가 있다면 해당 문진 결과에서 다시 첨부하세요.

요청 완료 기록은 `image_requests` 탭에 저장합니다. EMR 비밀번호는 스크립트 속성 `EMR_PASSWORD` 또는 기본값 `1824`입니다. 변경한 차트번호는 문진 연결키에 영향을 주지 않습니다. 환자 이름이나 원본 기록 시각을 직접 변경하면 자료 연결 이전 작업이 필요합니다.

## 검증과 참고

모의 API를 통한 브라우저 검사와 Apps Script 대체 환경에서 기록별 분리·입력 검증·반영 저장·OCR 파서를 검증합니다. 실제 Google OCR과 Drive 저장은 위 설정 및 재배포 후 확인해야 합니다.

공식 문서: [Drive 이미지 OCR 변환](https://developers.google.com/workspace/drive/api/guides/manage-uploads#import_to_google_docs_types), [Apps Script 고급 서비스](https://developers.google.com/apps-script/guides/services/advanced).


## 문진 기록 개별 삭제

EMR에서 환자를 선택한 뒤 각 문진 기록 오른쪽의 **삭제**를 누릅니다. 확인창에 표시된 환자명·종류·작성일을 확인하고 승인하면 선택한 한 건만 목록에서 삭제됩니다. 마지막 문진까지 삭제된 환자는 조회 목록에서도 사라집니다.

이 기능을 처음 적용하려면 최신 **Code.gs 전체 교체 → 저장 → 배포 → 배포 관리 → 연필 → 새 버전 → 배포** 후 `index.html`을 새로고침하세요. 삭제 기능만을 위해 추가 서비스나 설정 함수를 실행할 필요는 없습니다.

서버는 삭제할 원본 행을 `deleted_records` 탭에 먼저 백업합니다(앞의 두 열: 삭제 요청 ID와 삭제 시각, 이후 열: 기존 원본 행). 삭제 요청 결과는 `record_delete_requests`에 저장됩니다. 문진에 연결된 이미지와 OCR 원본은 자동 삭제하지 않으며 기존 저장소에 보존됩니다. 현재 화면에는 삭제 복원 기능이 없습니다.
