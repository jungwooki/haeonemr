# 문진 결과별 이미지 첨부

사용 순서: **HAEON EMR → 로그인 → 환자 및 문진 기록 선택 → 이미지 첨부 → 이미지를 클릭하여 원본 보기**.

- JPG, PNG, WEBP, GIF / 파일당 10MB / 문진 기록별 최대 3개.
- 업로드, 썸네일, 원본 미리보기, 삭제를 지원합니다.
- OCR, 자동 수치 추출 및 추출 결과 반영 기능은 제거했습니다. 판독결과 메모는 직접 작성할 수 있습니다.
- 기존 이미지 원본과 첨부 연결은 유지됩니다. 기존에 저장된 OCR 데이터는 삭제하지 않지만 조회 응답과 화면에는 포함하지 않습니다.

## 설정 및 배포

1. 소스 수정 후 프로젝트 최상위 폴더에서 `python3 emr/tools/build.py`를 실행합니다.
2. 구글시트의 **확장 프로그램 → Apps Script**에서 생성된 `emr/Code.gs` 전체로 교체하고 저장합니다.
3. 최초 설정 시 **setupEmrImages**를 실행하고 Drive 및 스프레드시트 권한을 허용합니다. 기존 이미지 기능을 사용 중이면 다시 실행할 필요가 없습니다.
4. **배포 → 배포 관리 → 기존 웹 앱 → 수정 → 새 버전 → 배포**를 진행합니다. 웹 앱 URL은 유지합니다.
5. 최신 `index.html`로 화면을 새로고침합니다.

OCR용 고급 Drive API와 Google 문서 접근은 더 이상 필요하지 않습니다. 기본 DriveApp은 원본 이미지 보관에 계속 사용합니다.

원본은 비공개 Drive 폴더에 보관하고 API를 통해 조회합니다. `record_images`는 기록별 첨부, `image_requests`는 저장 완료 확인에 사용합니다. `EMR_IMAGE_FOLDER_ID`와 기존 시트를 유지하세요. 서버 비밀번호는 스크립트 속성 `EMR_PASSWORD`를 사용합니다.

이미지 목록은 메모리에 1분간 보관합니다. 다른 기기의 변경을 즉시 확인하려면 **목록 새로고침**을 누르세요.
